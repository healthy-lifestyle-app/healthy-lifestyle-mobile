import { View, Text } from "react-native";

export default function RecipesScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 18 }}>Tarifler</Text>
    </View>
  );
}